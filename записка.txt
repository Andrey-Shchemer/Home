Пояснительная записка
доустановил чз "роли и компоненты" CGI
разархивировал (( использовал потоко-небезопасную (Non-thread-safe) сборку PHP)) php-8.1.6-nts-Win32-vs16-x64.zip в "c:\Program Files\PHP\.." 
переименовал в директории PHP файл "php.ini-production" в "php.ini"
ручками туда вписал:
;    open_basedir = C:\inetpub\wwwroot
;    fastcgi.impersonate = 1
;    fastcgi.logging = 0
;    cgi.fix_pathinfo=1
;    cgi.force_redirect = 0
проверил работу PHP (Скрин шот)
создал в папке c:\inetpub\wwwroot\Vereteno\index.php с текстом:
;   <?php
;
;   phpinfo();
;
;   ?>
создал устовие для *.PHP на уровне домена с ограничением вызывать только при сопоставлении с файлом или каталогом
загрузил http://vereteno.by.mydomain.com получил инфо о PHP (скрин шот)
разорхивировал вордпресс в папку самого сайта и получил пока чистый вордпесс  (скрин шот)
установил mysql-installer-community-5.6.28.0

донастройка, создание пользователя и базы данных ч-з командную строку

cmd

CD "C:\Program Files\MySQL\MySQL Server 8.0/bin"
mysql -u root -p
show databases
create database wordpress
create user 'user1' indetified by '12345678'
grant all on wordpress.* to 'user1'
exit
